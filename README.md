# Co2-notifier

## Requirements

    $ brew install hidapi

## Install

    $ gem install co2-notify

## Usage

    $ co2-notify init
    $ co2-notify go

## Autoload

    $ co2-notify autoload $(which co2-notify)
    $ co2-notify start
