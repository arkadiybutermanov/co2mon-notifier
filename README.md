# Co2-notifier

## Requirements

    $ brew install hidapi

## Install

    $ gem install co2-notify

## Usage

    $ co2-notify init
    $ co2-notify start

## Autoload

    $ co2-notify autoload
    $ launchctl load Library/LaunchAgents/arkadiybutermanov.co2-notify.plist
